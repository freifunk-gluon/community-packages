ffbs-debugbathosts
==================

This package provides `/etc/bat-hosts`.
This file can be used to map mac-addresses to names in `batctl`.

This file is empty by default and mainly serves as a reminder on how this
can be used on the devices itself.
